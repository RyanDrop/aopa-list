export interface Project {
    name: string;
    goal: string;
    endDate: string;
    icon: string;
    color: string;
    id: number
}

export interface CreateProject {
    name: string;
    goal: string;
    endDate: string;
    icon: string;
    color: string;
}